{% if things.size > 0 %}
{% assign types_title = schema_id | append: " type definitions" %}

{% render "templates/basic_title", thing_prefix: thing_prefix, anchor: "types", title: types_title, depth: depth %}

{% for thing in things %}
{% assign type_nested_depth = depth | plus: 1 %}

{% render "templates/type", thing: thing, thing_prefix: thing_prefix, depth: type_nested_depth %}

{% endfor %}

{% endif %}
