{% if things.size > 0 %}
{% assign subtype_constraints_title = schema_id | append: " subtype constraint definitions" %}

{% render "templates/basic_title", thing_prefix: thing_prefix, anchor: "subtype_constraints", title: subtype_constraints_title, depth: depth %}

{% assign sc_nested_depth = depth | plus: 1 %}
{% for thing in things %}

{% render "templates/basic_thing", thing: thing, thing_prefix: thing_prefix, depth: sc_nested_depth %}

{% endfor %}

{% endif %}
