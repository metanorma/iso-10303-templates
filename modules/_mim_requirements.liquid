
{% capture MIN_ENTITIES_INTRO %}
[type=express]
==== MIM entity definitions

This subclause specifies the MIM entities for this module.
The MIM entities and definitions are specified below.
{% endcapture %}

{% capture MIN_FUNCTIONS_INTRO %}
[type=express]
==== MIM function definitions

This subclause specifies the MIM functions for this module. The MIM functions and definitions are
specified below.
{% endcapture %}

{% capture MIN_TYPE_INTRO %}
[type=express]
==== MIM rule definitions

This subclause specifies the MIM types for this application module. The MIM types and definitions are specified below.
{% endcapture %}

{% capture MIN_RULES_INTRO %}
[type=express]
==== MIM rule definitions

This subclause specifies the MIM rules for this module. The MIM rules and definitions are specified below.
{% endcapture %}

{% render "templates/modules/module_mim_content", schema: schema %}

{% render "templates/modules/definitions", definitions: schema.entities, intro: MIN_ENTITIES_INTRO, schema_id: schema.id, depth: 4 %}

{% render "templates/modules/definitions", definitions: schema.functions, intro: MIN_FUNCTIONS_INTRO, schema_id: schema.id, depth: 4 %}

{% render "templates/modules/definitions", definitions: schema.types, intro: MIN_TYPE_INTRO, schema_id: schema.id, depth: 4 %}

{% render "templates/modules/definitions", definitions: schema.rules, intro: MIN_RULES_INTRO, schema_id: schema.id, depth: 4 %}

[source%unnumbered]
--
END_SCHEMA;  -- {{ schema.id }}
--
